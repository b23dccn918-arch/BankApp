<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<BorderPane prefHeight="600" prefWidth="1000"
            xmlns="http://javafx.com/javafx/17"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.mycompany.bankonline.Controller.History.HistoryController"
            styleClass="history-root">

    <!-- 🔷 HEADER -->
    <top>
        <AnchorPane prefHeight="70" styleClass="header-container">
            <children>
                <Label layoutX="30" layoutY="20" text="🏦 BASOUR BANK" styleClass="header-title"/>
                <Label layoutX="420" layoutY="25" text="History" styleClass="header-subtitle"/>
                <Button fx:id="logoutButton" layoutX="850" layoutY="22" text="Logout" styleClass="btn-logout"/>
            </children>
        </AnchorPane>
    </top>

    <!-- 🧭 SIDEBAR -->
    <left>
        <AnchorPane prefHeight="600" prefWidth="200"
                    styleClass="sidebar-container"
                    BorderPane.alignment="CENTER">
            <VBox spacing="15" alignment="TOP_CENTER"
                  AnchorPane.topAnchor="30"
                  AnchorPane.leftAnchor="0"
                  AnchorPane.rightAnchor="0">

                <Button fx:id="homeButton" text="🏠  Home" prefWidth="180" styleClass="sidebar-button"/>
                <Button fx:id="accountButton" text="👤  User Info" prefWidth="180" styleClass="sidebar-button"/>
                <Button fx:id="transferButton" text="💸  Transfer" prefWidth="180" styleClass="sidebar-button"/>
                <Button fx:id="withdrawButton" text="💰  Withdraw" prefWidth="180" styleClass="sidebar-button"/>
                <Button fx:id="depositButton" text="💵  Deposit" prefWidth="180" styleClass="sidebar-button"/>
                <Button fx:id="paymentButton" text="🧾  Payment" prefWidth="180" styleClass="sidebar-button"/>
                <Button fx:id="historyButton" text="📜  History" prefWidth="180" styleClass="sidebar-button"/>
            </VBox>
        </AnchorPane>
    </left>

    <!-- 📜 MAIN CONTENT -->
    <center>
        <VBox spacing="20" styleClass="main-content">

            <Label text="LỊCH SỬ GIAO DỊCH" styleClass="page-title"/>

            <!-- Bộ lọc ngày -->
            <HBox spacing="15" alignment="CENTER_LEFT">
                <Label text="Từ ngày:" styleClass="form-label"/>
                <DatePicker fx:id="fromDate" styleClass="date-picker"/>
                <Label text="Đến ngày:" styleClass="form-label"/>
                <DatePicker fx:id="toDate" styleClass="date-picker"/>
                <Button fx:id="filterButton" text="Lọc" styleClass="filter-button"/>
            </HBox>

            <!-- Bảng giao dịch -->
            <TableView fx:id="transactionTable" prefHeight="400" styleClass="table-view">
                <columns>
                    <TableColumn fx:id="colId" prefWidth="80" text="Mã GD"/>
                    <TableColumn fx:id="colFrom" prefWidth="120" text="Người gửi"/>
                    <TableColumn fx:id="colTo" prefWidth="120" text="Người nhận"/>
                    <TableColumn fx:id="colType" prefWidth="100" text="Loại"/>
                    <TableColumn fx:id="colAmount" prefWidth="120" text="Số tiền"/>
                    <TableColumn fx:id="colDescription" prefWidth="180" text="Mô tả"/>
                    <TableColumn fx:id="colDate" prefWidth="150" text="Ngày tạo"/>
                    <TableColumn fx:id="colStatus" prefWidth="100" text="Trạng thái"/>
                </columns>
            </TableView>

        </VBox>
    </center>
</BorderPane>
